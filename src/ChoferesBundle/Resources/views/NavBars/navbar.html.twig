{% if is_granted('IS_AUTHENTICATED_FULLY') %}
    {% if app.user.rol == 'ROLE_ADMIN' %}
        {{ include('ChoferesBundle:NavBars:role_admin.html.twig') }}
    {% endif %}
    {% if app.user.rol == 'ROLE_CNTSV' %}
        {{ include('ChoferesBundle:NavBars:role_cntsv.html.twig') }}
    {% endif %}
    {% if app.user.rol == 'ROLE_PRESTADOR' %}
        {{ include('ChoferesBundle:NavBars:role_prestador.html.twig') }}
    {% endif %}
    {% if app.user.rol == 'ROLE_CNRT' %}
        {{ include('ChoferesBundle:NavBars:role_cnrt.html.twig') }}
    {% endif %}
    {% if app.user.rol == 'ROLE_CENT' %}
        {{ include('ChoferesBundle:NavBars:role_cent.html.twig') }}
    {% endif %}
{% endif %}

<style>
    body {
      padding-top: 20px;
      padding-bottom: 40px;
    }
    /* Custom container */
    .container-narrow {
      margin: 0 auto;
      max-width: 700px;
    }
    .container-narrow > hr {
      margin: 30px 0;
    }
    .jumbotron {
      margin: 60px 0;
      text-align: center;
    }
    .jumbotron h1 {
      font-size: 72px;
      line-height: 1;
    }
</style>
