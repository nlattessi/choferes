{% extends 'JordiLlonchCrudGeneratorBundle::layout.html.twig' %}

{% block title %}
{{ parent() }} - {{ 'views.show.show'|trans({'%entity%': 'Chofer'}, 'JordiLlonchCrudGeneratorBundle') }}
{% endblock %}

{% block page %}

<h1>Chofer</h1>

<form class="form-horizontal">
    <fieldset>        <div class="control-group">
            <label class="control-label">Id</label>
            <div class="controls">                <span class="input-xlarge uneditable-input">{{ entity.id }}</span>            </div>
        </div>        <div class="control-group">
            <label class="control-label">Nombre</label>
            <div class="controls">                <span class="input-xlarge uneditable-input">{{ entity.nombre }}</span>            </div>
        </div>        <div class="control-group">
            <label class="control-label">Apellido</label>
            <div class="controls">                <span class="input-xlarge uneditable-input">{{ entity.apellido }}</span>            </div>
        </div>        <div class="control-group">
            <label class="control-label">Dni</label>
            <div class="controls">                <span class="input-xlarge uneditable-input">{{ entity.dni }}</span>            </div>
        </div>        <div class="control-group">
            <label class="control-label">Precuil</label>
            <div class="controls">                <span class="input-xlarge uneditable-input">{{ entity.precuil }}</span>            </div>
        </div>        <div class="control-group">
            <label class="control-label">Colacuil</label>
            <div class="controls">                <span class="input-xlarge uneditable-input">{{ entity.colacuil }}</span>            </div>
        </div>        <div class="control-group">
            <label class="control-label">Cuilempresa</label>
            <div class="controls">                <span class="input-xlarge uneditable-input">{{ entity.cuilEmpresa }}</span>            </div>
        </div>        <div class="control-group">
            <label class="control-label">Tienecursobasico</label>
            <div class="controls">                <span class="input-xlarge uneditable-input"><td>{% if entity.tieneCursoBasico %}true{% else %}false{% endif %}</td></span>            </div>
        </div>    </fieldset>
</form>

<div>
    <div class="float-left">
        <a class="btn" href="{{ path('chofer') }}">
            {{ 'views.recordactions.backtothelist'|trans({}, 'JordiLlonchCrudGeneratorBundle') }}
        </a>
    </div>
    <div class="float-left">
        <a class="btn btn-primary" href="{{ path('chofer_edit', { 'id': entity.id }) }}">
            {{ 'views.recordactions.edit'|trans({}, 'JordiLlonchCrudGeneratorBundle') }}
        </a>
    </div>
    <div class="float-left">
        <form action="{{ path('chofer_delete', { 'id': entity.id }) }}" method="post">
            <input type="hidden" name="_method" value="DELETE" />
            {{ form_widget(delete_form) }}
            <button class="btn btn-danger" type="submit" onclick="return confirm('{{ 'views.recordactions.confirm'|trans({}, 'JordiLlonchCrudGeneratorBundle') }}');">{{ 'views.recordactions.delete'|trans({}, 'JordiLlonchCrudGeneratorBundle') }}</button>
        </form>
    </div>
</div>
{% endblock %}
